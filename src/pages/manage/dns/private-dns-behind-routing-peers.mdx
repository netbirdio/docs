export const description = 'Configure private DNS servers behind routing peers'
import {Note} from "@/components/mdx"

# Private DNS Behind Routing Peers

This content has been moved to the [Internal DNS Servers](/manage/dns/internal-dns-servers#configuring-nameservers-for-internal-domains) page, which provides comprehensive guidance on configuring NetBird with internal DNS infrastructure.
